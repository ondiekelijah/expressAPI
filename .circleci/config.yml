version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/node:16.13.1

    steps:
      - checkout
      - run: sudo npm install -g npm@latest
      - run: npm install
      - run: npm test